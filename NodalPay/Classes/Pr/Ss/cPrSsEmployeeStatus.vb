' This Class has been autogenerated by Nodalsoft
' Do NOT adjust as it will be overwritten
' Generation Date : 13/02/2008 09:42:51
'
'
Public Class cPrSsEmployeeStatus
    '
    Inherits cPrSsEmployeeStatusDbTier
    '
    Private mEmpSta_Code As String
    Private mEmpSta_DescriptionL As String
    Private mEmpSta_DescriptionS As String
    Public Property EmpSta_Code() As String
        Get
            Return mEmpSta_Code
        End Get
        Set(ByVal Value As String)
            mEmpSta_Code = Value
        End Set
    End Property
    Public Property EmpSta_DescriptionL() As String
        Get
            Return mEmpSta_DescriptionL
        End Get
        Set(ByVal Value As String)
            mEmpSta_DescriptionL = Value
        End Set
    End Property
    Public Property EmpSta_DescriptionS() As String
        Get
            Return mEmpSta_DescriptionS
        End Get
        Set(ByVal Value As String)
            mEmpSta_DescriptionS = Value
        End Set
    End Property
    Public Sub New()
    End Sub
    Public Sub New(ByVal Dr As DataRow)
        If Not Dr Is Nothing Then
            LoadDataRow(Dr)
        End If
    End Sub
    Public Sub New(ByVal tEmpSta_Code As String)
        If tEmpSta_Code <> "" Then
            Init(tEmpSta_Code)
        End If
    End Sub
    Private Sub Init(ByVal tEmpSta_Code As String)
        Try
            Dim ds As DataSet
            ds = MyBase.GetByPK(tEmpSta_Code)
            If CheckDataSet(ds) Then
                LoadDataRow(ds.Tables(0).Rows(0))
            End If
        Catch ex As System.Exception
        End Try
    End Sub
    Private Sub LoadDataRow(ByVal dr As DataRow)
        mEmpSta_Code = DbNullToString(dr.Item(0))
        mEmpSta_DescriptionL = DbNullToString(dr.Item(1))
        mEmpSta_DescriptionS = DbNullToString(dr.Item(2))
    End Sub
    Public Shadows Function Delete(ByVal tEmpSta_Code As String) As Boolean
        Try
            Dim Counter As Integer
            Dim TableCount As Integer
            Dim RecordCount As Integer
            Dim TransStr As String = ""
            Dim ds As DataSet
            ds = MyBase.CheckDeleteRecords(tEmpSta_Code)
            If CheckDataSet(ds) Then
                For Counter = 0 To ds.Tables.Count - 1
                    TableCount = TableCount + 1
                    RecordCount = RecordCount + DbNullToInt(ds.Tables(Counter).Rows(0).Item(0))
                    TransStr = TransStr & vbCrLf & "Table " & TableCount & "  Records " & RecordCount
                Next Counter
                If RecordCount = 0 Then
                    Return MyBase.Delete(tEmpSta_Code)
                Else
                    MsgBox("Transactions Exist For This item - Can not Delete" & TransStr, MsgBoxStyle.Critical)
                End If
            Else
                MsgBox("Failed to confirm that no records exist for this item - Can not Delete")
                Return False
            End If
        Catch ex As System.Exception
        End Try
    End Function
    '
    Public Shadows Function Save() As Boolean
        Try
            Return MyBase.Save(Me)
        Catch ex As System.Exception
            Return False
        End Try
    End Function
    Public Overrides Function ToString() As String
        Return Me.EmpSta_Code & " - " & Me.EmpSta_DescriptionL
    End Function
End Class
