' This Class has been autogenerated by Nodalsoft
' Do NOT adjust as it will be overwritten
' Generation Date : 13/02/2008 09:42:48
'
'
Public Class cPrAnEmployeeCommunityDbTier
    '
    Inherits cDataTier
    '
    Protected Function GetByPK(ByVal tCode As String) As DataSet
        Dim Str As String
        Str = " SELECT" & _
                " EmpCmm_Code," & _
                " EmpCmm_DescriptionL," & _
                " EmpCmm_DescriptionS," & _
                " EmpCmm_IsActive," & _
                " EmpCmm_Sequence" & _
            "  FROM PrAnEmployeeCommunity" & _
            "  WHERE EmpCmm_Code = '" & tCode & "'"
        Return MyBase.GetData(Str)
    End Function
    Protected Function Save(ByVal _cPrAnEmployeeCommunity As cPrAnEmployeeCommunity) As Boolean
        Dim SpParams As New ArrayList
        Dim SpNames As New ArrayList
        Dim Flag As Boolean = False
        With _cPrAnEmployeeCommunity
            SpParams.Add(.Code)                                              '(0)
            SpNames.Add("EmpCmm_Code")                                       '(0)
            SpParams.Add(.DescriptionL)                                      '(1)
            SpNames.Add("EmpCmm_DescriptionL")                               '(1)
            SpParams.Add(.DescriptionS)                                      '(2)
            SpNames.Add("EmpCmm_DescriptionS")                               '(2)
            SpParams.Add(.IsActive)                                          '(3)
            SpNames.Add("EmpCmm_IsActive")                                   '(3)
            SpParams.Add(.Sequence)                                          '(4)
            SpNames.Add("EmpCmm_Sequence")                                   '(4)
        End With
        If Me.StoredProcedure("AG_PrAnEmployeeCommunity_Save_Update", SpParams, SpNames) Then
            Return True
        Else
            Return False
        End If
    End Function
    Protected Function Delete(ByVal tCode As String) As Boolean
        Dim Str As String
        Dim Flag As Boolean = True
        Dim Exx As New System.Exception
        Try
            BeginTransaction()
            Str = " DELETE FROM PrAnEmployeeCommunity" & _
               " WHERE EmpCmm_Code = '" & tCode & "'"
            If MyBase.ExecuteNonQuery(Str) = -1 Then
                Throw Exx
            End If
            CommitTransaction()
        Catch ex As Exception
            Rollback()
            Utils.ShowException(ex)
            Flag = False
        End Try
        Return Flag
    End Function
    Protected Function CheckDeleteRecords(ByVal tCode As String) As DataSet
        Dim Str As String
        Str = " " & _
        "SELECT COUNT(EmpCmm_Code) " & _
        " FROM PrMsEmployees" & _
        " WHERE EmpCmm_Code = '" & tCode & "'" & _
        " " & _
        " "
        Return GetData(Str)
    End Function
End Class
